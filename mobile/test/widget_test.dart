// App smoke test — updated for Story 1.3
// Full routing tests are in test/features/auth/login_screen_test.dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — routing tests in test/features/', () {
    expect(true, isTrue);
  });
}
