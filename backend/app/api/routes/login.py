# Email/password admin login — to be fully implemented in Story 5.1
# Kept here as placeholder; not registered in api/main.py router.
from fastapi import APIRouter

router = APIRouter(tags=["login"])
